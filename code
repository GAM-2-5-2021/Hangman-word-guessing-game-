import random

word_list =  ['Backpack', 'Hangman']


def get_word():
    word = random.choice(word_list)
    return word.upper()


def play(word):
    word_completion = "_" * len(word)
    print("Let's play Hangman!")
    print(word_completion)
    print("\n")
